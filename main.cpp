#include <bits/stdc++.h>
using namespace std;

void read_data(){

}

int main(int argc, char *argv[])
{
    if (argc == 3){
        freopen(argv[1], "r", stdin);
        freopen(argv[2], "w", stdout);
    } else
    {
        freopen("input.txt", "r", stdin);
    }
    read_data();

    return 0;
}
